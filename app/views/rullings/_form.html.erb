<%= form_for(@rulling) do |f| %>
  <% if @rulling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rulling.errors.count, "error") %> prohibited this rulling from being saved:</h2>

      <ul>
      <% @rulling.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :objective %><br>
    <%= f.text_field :objective %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
